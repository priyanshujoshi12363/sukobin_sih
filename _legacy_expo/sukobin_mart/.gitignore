# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
.env

# secrets — service-account keys ONLY (NEVER commit these)
# NOTE: google-services.json is intentionally NOT ignored — it's safe to commit
# (it's bundled in every APK) and EAS cloud builds need it uploaded with the project.
sukobin-37444-*.json
*serviceAccount*.json
*-firebase-adminsdk-*.json

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android
